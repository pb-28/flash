import React from "react";
import ReactDOM from "react-dom/client";
import FlashPickleballWebsite from "./flash-pickleball-site.tsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FlashPickleballWebsite />
  </React.StrictMode>
);
